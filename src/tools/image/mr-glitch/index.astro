---
---
<section class="stage">
  <div class="drop-zone" data-zone tabindex="0" role="button">
    <input type="file" accept="image/*" data-file hidden />
    <div class="dz-title">Drop an image or click to browse</div>
    <div class="dz-hint">PNG, JPEG, or WebP - nothing uploads.</div>
  </div>

  <div class="grid" data-controls style="display:none;grid-template-columns:repeat(auto-fit,minmax(180px,1fr))">
    <label class="field">
      <span class="field-label">Intensity: <span data-intensity-value>60</span>%</span>
      <input class="input" type="range" min="0" max="100" step="1" value="60" data-intensity />
    </label>
    <label class="field">
      <span class="field-label">Seed</span>
      <input class="input" type="number" value="42" data-seed />
    </label>
  </div>

  <figure data-preview style="display:none">
    <figcaption class="field-label" data-label>Preview</figcaption>
    <img data-out alt="Glitched image" />
  </figure>

  <div class="actions">
    <button class="btn btn-primary" type="button" data-download disabled>Download PNG</button>
  </div>
  <p class="error-text" data-error style="display:none"></p>
</section>

<style>
  figure {
    margin: 0;
  }
  figure img {
    max-width: 100%;
    border: 1px solid var(--border);
    border-radius: var(--radius-sm);
  }
</style>

<script>
  import { buildGlitchPlan, applyGlitch } from "./tool.ts";
  import { loadImageBitmap, imageDataOf, canvasToBlob } from "../_lib.ts";
  import { swapExtension } from "@/lib/files";
  import { downloadBlob } from "@/lib/download";

  const zone = document.querySelector<HTMLDivElement>("[data-zone]")!;
  const fileInput = document.querySelector<HTMLInputElement>("[data-file]")!;
  const intensityInput = document.querySelector<HTMLInputElement>("[data-intensity]")!;
  const intensityValue = document.querySelector<HTMLSpanElement>("[data-intensity-value]")!;
  const seedInput = document.querySelector<HTMLInputElement>("[data-seed]")!;
  const controls = document.querySelector<HTMLDivElement>("[data-controls]")!;
  const preview = document.querySelector<HTMLElement>("[data-preview]")!;
  const outImg = document.querySelector<HTMLImageElement>("[data-out]")!;
  const label = document.querySelector<HTMLElement>("[data-label]")!;
  const downloadBtn = document.querySelector<HTMLButtonElement>("[data-download]")!;
  const errorEl = document.querySelector<HTMLParagraphElement>("[data-error]")!;

  let bitmap: ImageBitmap | null = null;
  let fileName = "";
  let width = 0;
  let height = 0;
  let currentBlob: Blob | null = null;
  let outUrl = "";
  const canvas = document.createElement("canvas");
  const ctx = canvas.getContext("2d")!;

  zone.addEventListener("click", () => fileInput.click());
  zone.addEventListener("keydown", (e) => {
    if (e.key === "Enter" || e.key === " ") {
      e.preventDefault();
      fileInput.click();
    }
  });
  fileInput.addEventListener("change", () => {
    const file = fileInput.files?.[0];
    if (file) load(file);
    fileInput.value = "";
  });
  zone.addEventListener("dragover", (e) => {
    e.preventDefault();
    zone.classList.add("drag");
  });
  zone.addEventListener("dragleave", () => zone.classList.remove("drag"));
  zone.addEventListener("drop", (e) => {
    e.preventDefault();
    zone.classList.remove("drag");
    const file = e.dataTransfer?.files?.[0];
    if (file) load(file);
  });

  function setError(msg: string) {
    errorEl.textContent = msg;
    errorEl.style.display = msg ? "block" : "none";
  }

  async function load(file: File) {
    setError("");
    try {
      const img = await loadImageBitmap(file);
      bitmap = img.bitmap;
      fileName = file.name;
      width = img.width;
      height = img.height;
      controls.style.display = "";
      preview.style.display = "";
      render();
    } catch {
      setError("Could not read that file as an image.");
    }
  }

  async function render() {
    if (!bitmap) return;
    const seed = Math.floor(seedInput.valueAsNumber || 0);
    const intensity = intensityInput.valueAsNumber / 100;
    intensityValue.textContent = String(Math.round(intensity * 100));

    const src = imageDataOf(canvas, bitmap).data;
    const plan = buildGlitchPlan(width, height, seed, intensity);
    const glitched = applyGlitch(src, width, height, plan);

    const data = new ImageData(width, height);
    data.data.set(glitched);
    ctx.clearRect(0, 0, width, height);
    ctx.putImageData(data, 0, 0);
    currentBlob = await canvasToBlob(canvas, "image/png");
    if (outUrl) URL.revokeObjectURL(outUrl);
    outUrl = URL.createObjectURL(currentBlob);
    outImg.src = outUrl;
    label.textContent = `${width}×${height} - ${plan.length} slices, seed ${seed}`;
    downloadBtn.disabled = false;
  }

  intensityInput.addEventListener("input", render);
  seedInput.addEventListener("input", render);

  downloadBtn.addEventListener("click", () => {
    if (!currentBlob) return;
    downloadBlob(currentBlob, swapExtension(fileName, "png"));
  });
</script>
