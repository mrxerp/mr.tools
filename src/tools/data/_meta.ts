import type { FamilyMeta } from "../../types/tool";

export const meta: FamilyMeta = {
  id: "data",
  name: "mr.data",
  tagline: "Data format tools - JSON, XML, YAML, TOML, formatting, and validation.",
  description: "mr.data handles JSON, XML, YAML, and TOML with formatting, validation, querying, and conversion - all local, nothing ever uploads.",
  icon: "data",
};