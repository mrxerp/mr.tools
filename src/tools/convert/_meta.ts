import type { FamilyMeta } from "../../types/tool";

export const meta: FamilyMeta = {
  id: "convert",
  name: "mr.convert",
  tagline: "Archive, ebook, geo, mesh, encoding, calendar, document, contact, and favicon converters.",
  description: "mr.convert is a suite of file format converters that run entirely in your browser. Archives, ebooks, GPS data, 3D meshes, text encodings, calendars, documents, contacts, and favicons — nothing ever uploads.",
  icon: "convert",
};