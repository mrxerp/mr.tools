import type { FamilyMeta } from "../../types/tool";

export const meta: FamilyMeta = {
  id: "dev",
  name: "mr.dev",
  tagline: "Developer utilities — hashes, encoding, timestamps, regex, and more.",
  description: "mr.dev is a set of developer tools that run entirely in your browser. Hashing, encoding, timestamp conversion, regex building, and more — nothing ever uploads.",
  icon: "code",
};